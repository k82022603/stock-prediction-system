# GitHub & GitLab 완전 가이드
## 바이브코딩 시대의 필수 협업 플랫폼

---

## 📚 목차

1. [GitHub와 GitLab이란?](#1-github와-gitlab이란)
2. [GitHub 시작하기](#2-github-시작하기)
3. [GitHub에서 PR 만들기](#3-github에서-pr-만들기)
4. [GitHub PR 고급 기능](#4-github-pr-고급-기능)
5. [GitHub Actions (CI/CD)](#5-github-actions-cicd)
6. [GitHub Projects & Issues](#6-github-projects--issues)
7. [GitLab 시작하기](#7-gitlab-시작하기)
8. [GitLab에서 MR 만들기](#8-gitlab에서-mr-만들기)
9. [GitLab CI/CD](#9-gitlab-cicd)
10. [GitHub vs GitLab 비교](#10-github-vs-gitlab-비교)
11. [협업 워크플로우](#11-협업-워크플로우)
12. [보안 및 권한 관리](#12-보안-및-권한-관리)
13. [바이브코딩과의 통합](#13-바이브코딩과의-통합)
14. [FAQ](#14-faq)

---

## 1. GitHub와 GitLab이란?

### 1.1 정의

**GitHub**
- Git 저장소 호스팅 서비스
- Microsoft 소유
- 전 세계 가장 큰 개발자 커뮤니티
- 오픈소스의 중심

**GitLab**
- Git 저장소 호스팅 + DevOps 플랫폼
- GitLab Inc. 소유
- 오픈소스 (Community Edition)
- 셀프 호스팅 가능

### 1.2 핵심 기능 비교

| 기능 | GitHub | GitLab |
|------|--------|--------|
| **Git 호스팅** | ✅ | ✅ |
| **Pull/Merge Request** | Pull Request (PR) | Merge Request (MR) |
| **CI/CD** | GitHub Actions | GitLab CI/CD |
| **이슈 트래킹** | GitHub Issues | GitLab Issues |
| **프로젝트 관리** | GitHub Projects | GitLab Boards |
| **Wiki** | ✅ | ✅ |
| **패키지 레지스트리** | GitHub Packages | GitLab Package Registry |
| **보안 스캔** | Dependabot, CodeQL | Security Dashboard |
| **셀프 호스팅** | ❌ (Enterprise만) | ✅ (Community부터) |
| **가격** | 무료 (Public), $4/월 | 무료 (제한적), $19/월 |

### 1.3 사용 통계 (2025년 기준)

```
GitHub:
- 사용자: 1억+ 명
- 저장소: 4억+ 개
- 기업 사용자: 90%+
- 오픈소스: 대부분

GitLab:
- 사용자: 3천만+ 명
- 저장소: 5천만+ 개
- 기업 사용자: 50%
- 특히 DevOps 중시 기업
```

### 1.4 어떤 것을 선택할까?

**GitHub를 선택하는 경우:**
```
✅ 오픈소스 프로젝트
✅ 개발자 커뮤니티 중시
✅ GitHub Actions 활용
✅ Microsoft 생태계 (Azure, VS Code)
✅ 간단한 CI/CD
```

**GitLab을 선택하는 경우:**
```
✅ 강력한 CI/CD 필요
✅ 셀프 호스팅 필요 (보안, 규정)
✅ 통합 DevOps 플랫폼
✅ 비공개 프로젝트 (무료 플랜)
✅ Kubernetes 네이티브
```

---

## 2. GitHub 시작하기

### 2.1 계정 생성

#### Step 1: 회원가입

```
1. https://github.com 접속
2. "Sign up" 버튼 클릭
3. 이메일 입력
4. 비밀번호 생성 (최소 15자 또는 8자+숫자/특수문자)
5. 사용자명 입력 (고유해야 함)
   - 예: john-developer, sarah-codes
6. 이메일 인증
7. 설문 조사 (선택)
```

#### Step 2: 프로필 설정

```
1. 우측 상단 프로필 이미지 클릭
2. "Settings" 선택
3. Profile 탭:
   - Name: 실명 또는 닉네임
   - Bio: 간단한 소개
   - Company: 회사명
   - Location: 위치
   - Website: 개인 웹사이트
   - Twitter: Twitter 계정
   - Profile Picture: 프로필 사진 업로드
```

#### Step 3: SSH 키 설정 (선택, 하지만 권장)

**왜 SSH 키?**
- 매번 비밀번호 입력 불필요
- 더 안전함
- Git 작업 속도 빠름

**생성 방법:**

```bash
# 1. SSH 키 생성
ssh-keygen -t ed25519 -C "your_email@example.com"

# 출력:
# Generating public/private ed25519 key pair.
# Enter file in which to save the key (/Users/you/.ssh/id_ed25519):
# → 그냥 Enter (기본 경로 사용)

# Enter passphrase (empty for no passphrase):
# → Enter (또는 비밀번호 입력)

# 2. SSH 에이전트 시작
eval "$(ssh-agent -s)"

# 3. SSH 키 추가
ssh-add ~/.ssh/id_ed25519

# 4. 공개 키 복사
# macOS:
pbcopy < ~/.ssh/id_ed25519.pub

# Windows (Git Bash):
cat ~/.ssh/id_ed25519.pub | clip

# Linux:
cat ~/.ssh/id_ed25519.pub
# 출력된 내용을 복사
```

**GitHub에 SSH 키 추가:**

```
1. GitHub 우측 상단 프로필 → Settings
2. 왼쪽 메뉴 "SSH and GPG keys"
3. "New SSH key" 버튼
4. Title: "My MacBook Pro" (기기 이름)
5. Key: 복사한 공개 키 붙여넣기
6. "Add SSH key" 클릭
```

**테스트:**

```bash
ssh -T git@github.com

# 출력:
# Hi username! You've successfully authenticated, but GitHub does not provide shell access.
```

### 2.2 저장소 생성

#### 방법 1: 웹에서 생성

```
1. GitHub 홈페이지 (로그인 후)
2. 우측 상단 "+" → "New repository"
3. 정보 입력:
   - Repository name: stock-prediction-system
   - Description: AI-powered stock prediction platform
   - Public / Private 선택
   - Initialize this repository with:
     ☑ Add a README file
     ☑ Add .gitignore (Java, Node 등 선택)
     ☐ Choose a license (MIT, Apache 등)
4. "Create repository" 클릭
```

#### 방법 2: CLI로 기존 프로젝트 업로드

```bash
# 로컬 프로젝트 디렉토리에서
cd stock-prediction-system

# Git 초기화
git init

# GitHub 저장소를 원격으로 추가
git remote add origin https://github.com/username/stock-prediction-system.git
# 또는 SSH:
git remote add origin git@github.com:username/stock-prediction-system.git

# 파일 추가
git add .

# 첫 커밋
git commit -m "Initial commit"

# 메인 브랜치 이름 설정 (main으로)
git branch -M main

# 푸시
git push -u origin main
```

### 2.3 저장소 클론

```bash
# HTTPS 방식
git clone https://github.com/username/stock-prediction-system.git

# SSH 방식 (추천)
git clone git@github.com:username/stock-prediction-system.git

# 특정 디렉토리에 클론
git clone git@github.com:username/stock-prediction-system.git my-project

# 특정 브랜치만 클론
git clone -b develop git@github.com:username/stock-prediction-system.git
```

### 2.4 GitHub 웹 인터페이스 둘러보기

#### 저장소 홈

```
┌────────────────────────────────────────────────────────┐
│  username / stock-prediction-system          ⭐ Star   │
│  📁 Public                                    👁 Watch  │
├────────────────────────────────────────────────────────┤
│  [Code] [Issues] [Pull requests] [Actions] [Projects] │
├────────────────────────────────────────────────────────┤
│  main ▼  [+ Add file ▼]  [<> Code ▼]                 │
├────────────────────────────────────────────────────────┤
│  📂 backend/                      2 hours ago          │
│  📂 frontend/                     2 hours ago          │
│  📂 database/                     2 hours ago          │
│  📄 README.md                     2 hours ago          │
│  📄 .gitignore                    2 hours ago          │
├────────────────────────────────────────────────────────┤
│  About                                                 │
│  AI-powered stock prediction platform                 │
│                                                        │
│  🏷 Topics: spring-boot, react, postgresql, ai        │
│                                                        │
│  📊 12 commits  🌿 3 branches  📋 5 issues            │
└────────────────────────────────────────────────────────┘
```

#### 탭 설명

| 탭 | 용도 |
|-----|------|
| **Code** | 파일 브라우저, README |
| **Issues** | 버그, 기능 요청, 토론 |
| **Pull requests** | PR 목록 및 관리 |
| **Actions** | CI/CD 워크플로우 |
| **Projects** | 프로젝트 보드 (칸반) |
| **Wiki** | 문서 작성 |
| **Security** | 보안 이슈 및 정책 |
| **Insights** | 통계 및 분석 |
| **Settings** | 저장소 설정 |

---

## 3. GitHub에서 PR 만들기

### 3.1 전체 과정 (상세 버전)

```
로컬 작업
  ↓
1. 브랜치 생성
  ↓
2. 코드 작성
  ↓
3. Commit
  ↓
4. Push
  ↓
GitHub 웹
  ↓
5. PR 생성 페이지 진입
  ↓
6. PR 정보 입력
  ↓
7. Reviewers 지정
  ↓
8. Labels, Projects 설정
  ↓
9. PR 생성
  ↓
10. CI/CD 자동 실행
  ↓
11. Code Review
  ↓
12. Approve
  ↓
13. Merge
```

### 3.2 Step-by-Step 가이드 (스크린샷 설명)

#### Step 1: 로컬에서 브랜치 생성 및 Push

```bash
# 최신 main 받기
git checkout main
git pull origin main

# 새 브랜치 생성
git checkout -b feature/add-portfolio-api

# 코드 작성 (AI 도구 또는 수동)
# ...

# 스테이징
git add .

# 커밋
git commit -m "feat: Add portfolio creation API

- Implement POST /api/portfolios endpoint
- Add PortfolioService with business logic
- Add MyBatis mapper and XML
- Write unit tests with 85% coverage"

# Push
git push origin feature/add-portfolio-api
```

**출력:**
```
Enumerating objects: 25, done.
Counting objects: 100% (25/25), done.
Delta compression using up to 8 threads
Compressing objects: 100% (15/15), done.
Writing objects: 100% (15/15), 3.45 KiB | 3.45 MiB/s, done.
Total 15 (delta 8), reused 0 (delta 0), pack-reused 0
remote: Resolving deltas: 100% (8/8), completed with 5 local objects.
remote: 
remote: Create a pull request for 'feature/add-portfolio-api' on GitHub by visiting:
remote:      https://github.com/username/stock-prediction-system/pull/new/feature/add-portfolio-api
remote: 
To github.com:username/stock-prediction-system.git
 * [new branch]      feature/add-portfolio-api -> feature/add-portfolio-api
```

#### Step 2: GitHub 웹에서 PR 생성 페이지로 이동

**방법 1: 출력된 링크 사용 (가장 빠름)**
```
위 출력에서 링크 복사:
https://github.com/username/stock-prediction-system/pull/new/feature/add-portfolio-api

브라우저에서 열기
```

**방법 2: GitHub 웹에서 찾기**
```
1. GitHub 저장소로 이동
   https://github.com/username/stock-prediction-system

2. 상단에 노란색 배너 표시됨:
   ┌─────────────────────────────────────────────────┐
   │ feature/add-portfolio-api had recent pushes    │
   │ 2 minutes ago                                   │
   │                        [Compare & pull request] │
   └─────────────────────────────────────────────────┘

3. "Compare & pull request" 버튼 클릭
```

**방법 3: Pull requests 탭에서**
```
1. "Pull requests" 탭 클릭
2. "New pull request" 버튼 클릭
3. base: main ← compare: feature/add-portfolio-api 선택
4. "Create pull request" 버튼
```

#### Step 3: PR 생성 화면 (상세 설명)

```
┌───────────────────────────────────────────────────────────┐
│  Open a pull request                                      │
├───────────────────────────────────────────────────────────┤
│  Create a new pull request by comparing changes across    │
│  two branches. If you need to, you can also compare       │
│  across forks.                                            │
├───────────────────────────────────────────────────────────┤
│  base: main    ←    compare: feature/add-portfolio-api   │
│  ✓ Able to merge                                          │
├───────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐ │
│  │ Title *                                             │ │
│  │ ┌─────────────────────────────────────────────────┐ │ │
│  │ │ feat: Add portfolio creation API                │ │ │
│  │ └─────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ [Write] [Preview]                                   │ │
│  │ ┌─────────────────────────────────────────────────┐ │ │
│  │ │ ## Description                                  │ │ │
│  │ │ Implements portfolio creation feature for       │ │ │
│  │ │ users to manage their stock holdings.           │ │ │
│  │ │                                                 │ │ │
│  │ │ ## Changes                                      │ │ │
│  │ │ - Added PortfolioController for REST API       │ │ │
│  │ │ - Added PortfolioService with business logic   │ │ │
│  │ │ - Added MyBatis mapper and XML                 │ │ │
│  │ │ - Added unit tests (85% coverage)              │ │ │
│  │ │                                                 │ │ │
│  │ │ ## Testing                                      │ │ │
│  │ │ - [x] Unit tests added                          │ │ │
│  │ │ - [x] Integration tests pass                    │ │ │
│  │ │ - [x] Manual testing with Postman              │ │ │
│  │ │                                                 │ │ │
│  │ │ ## Checklist                                    │ │ │
│  │ │ - [x] Code follows style guidelines            │ │ │
│  │ │ - [x] Self-review performed                     │ │ │
│  │ │ - [x] Tests added                               │ │ │
│  │ │ - [x] All tests pass                            │ │ │
│  │ └─────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  📎 Attach files by dragging & dropping, selecting or    │
│     pasting them.                                         │
├───────────────────────────────────────────────────────────┤
│  Reviewers             Assignees         Labels          │
│  ┌─────────────────┐  ┌──────────────┐  ┌─────────────┐│
│  │ 🔍 No one—      │  │ 👤 No one—   │  │ 🏷 None—    ││
│  │ assign yourself │  │ assign       │  │             ││
│  │                 │  │ yourself     │  │             ││
│  │ Team members:   │  │              │  │ Suggestions:││
│  │ ☐ @john         │  │              │  │ enhancement ││
│  │ ☐ @sarah        │  │              │  │ backend     ││
│  │ ☐ @mike         │  │              │  │ needs-review││
│  └─────────────────┘  └──────────────┘  └─────────────┘│
│                                                           │
│  Projects              Milestone         Development     │
│  ┌─────────────────┐  ┌──────────────┐  ┌─────────────┐│
│  │ None yet        │  │ No milestone │  │ Closes #123 ││
│  │                 │  │              │  │             ││
│  │ Sprint 5        │  │ v1.2.0       │  │             ││
│  │ Backlog         │  │ v1.3.0       │  │             ││
│  └─────────────────┘  └──────────────┘  └─────────────┘│
├───────────────────────────────────────────────────────────┤
│  ☐ Allow edits by maintainers                            │
│     Maintainers can edit your branch before merging      │
│                                                           │
│  ☐ Create a draft pull request                           │
│     This pull request is a work in progress               │
├───────────────────────────────────────────────────────────┤
│            [Create pull request] [Create draft PR]        │
└───────────────────────────────────────────────────────────┘
```

#### Step 4: 각 항목 입력하기

**① Title (필수)**
```
형식: <type>: <subject>

예시:
feat: Add portfolio creation API
fix: Fix null pointer exception in StockService
refactor: Extract duplicate code in controllers
docs: Update API documentation
test: Add integration tests for portfolio
```

**② Description (필수)**
```markdown
## Description
사용자가 자신의 주식 포트폴리오를 생성할 수 있는 기능을 추가했습니다.

## Changes
- **PortfolioController**: POST /api/portfolios 엔드포인트 추가
- **PortfolioService**: 포트폴리오 생성 및 중복 체크 로직
- **PortfolioMapper**: MyBatis 매퍼 인터페이스 및 XML
- **Portfolio**: 도메인 모델
- **PortfolioServiceTest**: 85% 커버리지 단위 테스트

## Type of Change
- [x] 새 기능 (feat)
- [ ] 버그 수정 (fix)
- [ ] Breaking change
- [ ] 문서 업데이트

## Testing
- [x] 단위 테스트 추가
- [x] 통합 테스트 통과
- [x] 수동 테스트 완료 (Postman)

## API Specification
\```json
POST /api/portfolios
Authorization: Bearer <token>

Request:
{
  "name": "내 포트폴리오",
  "description": "장기 투자용"
}

Response: 201 Created
{
  "id": 1,
  "userId": 123,
  "name": "내 포트폴리오",
  "description": "장기 투자용",
  "isDefault": false,
  "createdAt": "2025-12-07T00:00:00"
}
\```

## Screenshots
(UI 변경이 있다면 스크린샷 첨부)

## Checklist
- [x] 코드가 스타일 가이드를 따름
- [x] 자체 코드 리뷰 완료
- [x] 복잡한 로직에 주석 추가
- [x] 문서 업데이트 (필요 시)
- [x] 테스트 추가
- [x] 모든 테스트 통과

## Related Issues
Closes #123
Related to #456

## Generated by
Claude Code / Cursor / Manual
```

**③ Reviewers (중요!)**
```
클릭하면 팀원 목록 표시:
☐ @john-senior-dev (선임 개발자)
☐ @sarah-tech-lead (테크 리드)
☐ @mike-backend (백엔드 전문가)

최소 1명, 권장 2-3명 선택

팁:
- 코드 주인(code owner)을 우선 지정
- 해당 영역 전문가 포함
- 너무 많으면 책임 분산 → 1-3명 적정
```

**④ Assignees**
```
보통 PR 작성자 본인을 지정:
☑ @myself (나 자신)

의미:
- 이 PR의 책임자
- 피드백 반영 담당자
```

**⑤ Labels**
```
프로젝트에 따라 다르지만 일반적:
☑ enhancement (새 기능)
☑ backend (백엔드 관련)
☐ frontend
☐ bug (버그 수정)
☐ documentation
☐ needs-review (리뷰 필요)
☐ work-in-progress (작업 중)

여러 개 선택 가능
```

**⑥ Projects**
```
GitHub Projects 보드에 추가:
☑ Sprint 5

칸반 보드에 자동 추가됨
```

**⑦ Milestone**
```
릴리스 버전과 연결:
☑ v1.2.0

해당 마일스톤의 진행 상황에 반영
```

**⑧ Development**
```
Issue와 연결:
Closes #123
Fixes #456
Resolves #789

PR 머지 시 자동으로 Issue도 닫힘
```

**⑨ 추가 옵션**
```
☑ Allow edits by maintainers
  → 메인테이너가 브랜치를 수정 가능 (권장)

☐ Create a draft pull request
  → 작업 중이라 리뷰 준비 안 됨 (선택)
```

#### Step 5: PR 생성 버튼 클릭

```
[Create pull request] 버튼 클릭

또는

[Create draft pull request] (작업 중일 때)
```

### 3.3 PR 생성 후 화면

```
┌───────────────────────────────────────────────────────────┐
│  feat: Add portfolio creation API #45                     │
│  🟢 Open  @myself wants to merge 8 commits into main from │
│  feature/add-portfolio-api                                │
├───────────────────────────────────────────────────────────┤
│  [Conversation] [Commits] [Checks] [Files changed]       │
├───────────────────────────────────────────────────────────┤
│  @myself created 2 minutes ago                            │
│  ┌───────────────────────────────────────────────────┐   │
│  │ ## Description                                    │   │
│  │ Implements portfolio creation feature...          │   │
│  │ [전체 설명]                                        │   │
│  └───────────────────────────────────────────────────┘   │
├───────────────────────────────────────────────────────────┤
│  ⚙️ CI/CD Checks                                          │
│  ✅ Build and Test — Passed in 2m 34s                    │
│  ✅ Code Quality (SonarQube) — Passed                    │
│  ✅ Security Scan — Passed                                │
│  ⏳ Deploy to Staging — In progress                      │
├───────────────────────────────────────────────────────────┤
│  👥 Reviewers                                             │
│  @john-senior-dev (requested)                             │
│  @sarah-tech-lead (requested)                             │
├───────────────────────────────────────────────────────────┤
│  💬 Leave a comment                                       │
│  ┌───────────────────────────────────────────────────┐   │
│  │ Add your comment here...                          │   │
│  └───────────────────────────────────────────────────┘   │
│  [Comment] [Close pull request]                           │
├───────────────────────────────────────────────────────────┤
│  📊 This branch is 8 commits ahead of main                │
│  Merge pull request by using one of the merge strategies  │
│  available for this repository.                           │
│                                                           │
│  Squash and merge:                                        │
│  ● Combine all commits into one                           │
│  ● Cleaner history                                        │
│                                                           │
│  ⚠️ Merging is blocked                                    │
│  Required reviews: 0/2 approved                           │
│                                                           │
│  [Merge pull request ▼]  (비활성화)                       │
└───────────────────────────────────────────────────────────┘
```

---

## 4. GitHub PR 고급 기능

### 4.1 Code Review 기능

#### ① 파일 변경 사항 보기

**"Files changed" 탭 클릭:**

```
┌───────────────────────────────────────────────────────────┐
│  [Conversation] [Commits] [Checks] [Files changed (12)]  │
├───────────────────────────────────────────────────────────┤
│  Showing 12 changed files with 456 additions and 23       │
│  deletions.                                               │
│                                                           │
│  [👁 Viewed] [💬 Conversations] [▼ Filter] [⚙️ Settings]  │
├───────────────────────────────────────────────────────────┤
│  📄 backend/pom.xml                       +5  -2          │
│  ┌───────────────────────────────────────────────────┐   │
│  │ @@ -45,6 +45,9 @@                                 │   │
│  │  <dependency>                                     │   │
│  │    <groupId>org.springframework.boot</groupId>    │   │
│  │    <artifactId>spring-boot-starter-web</artifactId>│  │
│  │ + </dependency>                                    │   │
│  │ + <dependency>                                     │   │
│  │ +   <groupId>org.mybatis.spring.boot</groupId>    │   │
│  │ +   <artifactId>mybatis-spring-boot-starter</artifactId>│
│  └───────────────────────────────────────────────────┘   │
│                                                           │
│  📄 .../controller/PortfolioController.java  +89  -0     │
│  ┌───────────────────────────────────────────────────┐   │
│  │ +package com.stock.controller;                    │   │
│  │ +                                                 │   │
│  │ +import org.springframework.web.bind.annotation.*;│  │
│  │ +                                                 │   │
│  │ +@RestController                                   │   │
│  │ +@RequestMapping("/api/portfolios")               │   │
│  │ +public class PortfolioController {               │   │
│  │ +    // ...                                        │   │
│  │ +}                                                 │   │
│  └───────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────┘
```

#### ② 코드에 코멘트 달기

**특정 라인에 마우스 오버 → "+" 버튼 클릭:**

```
Line 67:  public Portfolio createPortfolio(...) {
          [+] ← 클릭

┌─────────────────────────────────────────────────────┐
│ 💬 Add a comment to this line                       │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 여기서 userId가 null일 수 있습니다.              │ │
│ │ null 체크를 추가하면 좋을 것 같습니다.           │ │
│ │                                                 │ │
│ │ 제안:                                            │ │
│ │ ```java                                         │ │
│ │ if (userId == null) {                           │ │
│ │     throw new IllegalArgumentException(...);    │ │
│ │ }                                               │ │
│ │ ```                                             │ │
│ └─────────────────────────────────────────────────┘ │
│ [Start a review] [Add single comment]               │
└─────────────────────────────────────────────────────┘
```

**차이점:**
- **Start a review**: 여러 코멘트를 모아서 한 번에 제출
- **Add single comment**: 즉시 제출

#### ③ 코드 제안 (Suggestion)

```
Line 89:  portfolio.setName(name);

💬 코멘트:
```suggestion
portfolio.setName(StringUtils.trim(name));
```

→ PR 작성자가 "Commit suggestion" 버튼 클릭하면 자동 적용!
```

#### ④ 리뷰 완료

**우측 상단 "Review changes" 버튼:**

```
┌─────────────────────────────────────────────────────┐
│ Review changes                                      │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 전체적으로 잘 작성되었습니다!                    │ │
│ │ 몇 가지 개선 사항을 제안했습니다.                │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ ● Comment (코멘트만)                                │
│ ○ Approve (승인)                                    │
│ ○ Request changes (수정 요청)                        │
│                                                     │
│ [Submit review]                                      │
└─────────────────────────────────────────────────────┘
```

**선택지:**
- **Comment**: 단순 의견, 승인/거부 없음
- **Approve**: 승인, 병합 가능
- **Request changes**: 수정 필요, 병합 불가

### 4.2 자동 검사 (Checks)

#### GitHub Actions 예시

**"Checks" 탭:**

```
┌───────────────────────────────────────────────────────────┐
│  All checks have passed                                   │
├───────────────────────────────────────────────────────────┤
│  ✅ Build and Test (ubuntu-latest, 17)                    │
│     Completed in 2m 34s                                   │
│     [Details]                                              │
│                                                           │
│  ✅ Code Quality                                          │
│     Completed in 1m 12s                                   │
│     Coverage: 85% (+2% from main)                         │
│     [Details]                                              │
│                                                           │
│  ✅ Security Scan                                          │
│     Completed in 45s                                      │
│     No vulnerabilities found                              │
│     [Details]                                              │
│                                                           │
│  ⏳ Deploy to Staging                                     │
│     In progress... (1m 20s)                               │
│     [Details]                                              │
└───────────────────────────────────────────────────────────┘
```

**Details 클릭 시:**

```
Job: Build and Test
├─ Set up job (15s)
├─ Run actions/checkout@v3 (3s)
├─ Set up JDK 17 (12s)
├─ Cache Maven packages (2s)
├─ Build with Maven (1m 20s)
│  └─ mvn clean package ✅
├─ Run tests (40s)
│  └─ mvn test ✅
│     Tests run: 156, Failures: 0, Errors: 0, Skipped: 0
└─ Complete job (2s)
```

### 4.3 PR 템플릿 (자동화)

#### 저장소에 템플릿 추가

**`.github/pull_request_template.md` 파일 생성:**

```markdown
## Description
<!-- 이 PR이 무엇을 하는지 간단히 설명 -->

## Changes
<!-- 변경사항 목록 -->
- 
- 
- 

## Type of Change
<!-- 해당하는 것에 체크 -->
- [ ] 새 기능 (feat)
- [ ] 버그 수정 (fix)
- [ ] Breaking change (기존 기능에 영향)
- [ ] 문서 업데이트 (docs)
- [ ] 리팩토링 (refactor)
- [ ] 테스트 (test)

## Testing
<!-- 어떻게 테스트했는지 -->
- [ ] 단위 테스트 추가
- [ ] 통합 테스트 통과
- [ ] 수동 테스트 완료

## Screenshots
<!-- UI 변경 시 스크린샷 -->

## Checklist
<!-- 모두 체크되어야 병합 가능 -->
- [ ] 코드가 스타일 가이드를 따름
- [ ] 자체 코드 리뷰 완료
- [ ] 주석 추가 (복잡한 로직)
- [ ] 문서 업데이트 (필요 시)
- [ ] 테스트 추가
- [ ] 모든 테스트 통과
- [ ] Breaking change 문서화 (해당 시)

## Related Issues
<!-- Issue 번호 -->
Closes #
```

**효과:**
- PR 생성 시 자동으로 템플릿 로드
- 일관된 PR 형식 유지
- 필수 정보 누락 방지

### 4.4 Draft PR

**작업 중 피드백이 필요할 때:**

```
PR 생성 시:
☑ Create a draft pull request

또는

기존 PR을 Draft로 변경:
우측 사이드바 → "Convert to draft"
```

**특징:**
- 🚧 Draft 표시
- 병합 불가능
- CI/CD 실행됨
- 리뷰 가능
- "Ready for review" 클릭 시 일반 PR로 전환

### 4.5 Merge 전략

**3가지 병합 방식:**

#### ① Merge Commit (기본)

```
feature/add-portfolio-api → main

Git 히스토리:
main:     A---B---C---D---H
                     \     /
feature:              E-F-G

결과:
- 모든 커밋 유지
- 명시적인 병합 커밋 (H)
- 히스토리가 복잡할 수 있음
```

**사용 시기:**
- 기능 개발 과정 추적 필요
- 브랜치 히스토리 보존 중요
- 롤백 시 한 번에 되돌리기 쉬움

#### ② Squash and Merge

```
feature의 모든 커밋을 하나로 합침:

main:     A---B---C---D---E
                         ↑
                    (E-F-G 합쳐짐)

결과:
- 깔끔한 히스토리
- 하나의 커밋으로 정리
- 세부 커밋 이력은 사라짐
```

**사용 시기:**
- 깔끔한 히스토리 선호
- 작은 PR (커밋 수 많을 때)
- 커밋 메시지 정리 필요

**추천!** 대부분의 경우 이 방식 사용

#### ③ Rebase and Merge

```
feature 커밋들을 main 위에 재배치:

main:     A---B---C---D---E---F---G

결과:
- 선형 히스토리
- 병합 커밋 없음
- 각 커밋 유지
```

**사용 시기:**
- 완전한 선형 히스토리 원할 때
- 각 커밋이 의미 있을 때

---

## 5. GitHub Actions (CI/CD)

### 5.1 GitHub Actions란?

**정의:**
- GitHub의 내장 CI/CD 플랫폼
- YAML 파일로 워크플로우 정의
- PR 생성/업데이트 시 자동 실행
- 무료 (Public 저장소: 무제한, Private: 월 2,000분)

### 5.2 기본 워크플로우 설정

#### Java (Spring Boot) 프로젝트

**`.github/workflows/ci.yml`:**

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build with Maven
      run: mvn clean package -DskipTests
      working-directory: ./backend
      
    - name: Run tests
      run: mvn test
      working-directory: ./backend
      
    - name: Generate test coverage
      run: mvn jacoco:report
      working-directory: ./backend
      
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        files: ./backend/target/site/jacoco/jacoco.xml
        
  code-quality:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        
  security-scan:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Run Snyk Security Scan
      uses: snyk/actions/maven@master
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      with:
        args: --severity-threshold=high
```

#### React (Frontend) 프로젝트

**`.github/workflows/frontend.yml`:**

```yaml
name: Frontend CI

on:
  pull_request:
    paths:
      - 'frontend/**'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
        
    - name: Install dependencies
      run: npm ci
      working-directory: ./frontend
      
    - name: Run linter
      run: npm run lint
      working-directory: ./frontend
      
    - name: Run tests
      run: npm test -- --coverage
      working-directory: ./frontend
      
    - name: Build
      run: npm run build
      working-directory: ./frontend
```

### 5.3 PR에서 확인

**PR의 "Checks" 탭에서 실시간 확인:**

```
✅ CI/CD Pipeline / build-and-test (pull_request)
   Completed in 2m 34s
   
✅ CI/CD Pipeline / code-quality (pull_request)
   Completed in 1m 12s
   
✅ CI/CD Pipeline / security-scan (pull_request)
   Completed in 45s
   
✅ Frontend CI / test (pull_request)
   Completed in 1m 05s
```

### 5.4 Status Checks 필수화

**Settings → Branches → Branch protection rules:**

```
☑ Require status checks to pass before merging
  ☑ Require branches to be up to date before merging
  
Status checks found in the last week for this repository:
  ☑ CI/CD Pipeline / build-and-test
  ☑ CI/CD Pipeline / code-quality
  ☑ Frontend CI / test
```

**효과:**
- 체크 실패 시 병합 불가능
- 코드 품질 자동 보장

---

## 6. GitHub Projects & Issues

### 6.1 GitHub Issues

#### Issue 생성

```
1. "Issues" 탭 클릭
2. "New issue" 버튼
3. 정보 입력:

Title:
포트폴리오 생성 API 구현

Description:
사용자가 자신의 포트폴리오를 생성할 수 있는 API가 필요합니다.

### 요구사항
- POST /api/portfolios
- JWT 인증 필요
- 이름 중복 체크
- 테스트 코드 포함

### Acceptance Criteria
- [ ] API 엔드포인트 구현
- [ ] 단위 테스트 작성
- [ ] 통합 테스트 작성
- [ ] API 문서 업데이트

Labels: enhancement, backend
Assignees: @myself
Milestone: v1.2.0
```

#### Issue와 PR 연결

**PR 설명에:**
```markdown
Closes #123
Fixes #456
Resolves #789
```

**효과:**
- PR 머지 시 자동으로 Issue 닫힘
- Issue에서 연결된 PR 확인 가능

### 6.2 GitHub Projects (칸반 보드)

#### 프로젝트 생성

```
1. "Projects" 탭 클릭
2. "New project" 버튼
3. 템플릿 선택:
   - Table (테이블 뷰)
   - Board (칸반 보드) ← 추천
   - Roadmap (로드맵)
```

#### 칸반 보드 구성

```
┌──────────┬──────────┬──────────┬──────────┬──────────┐
│ Backlog  │  To Do   │In Progress│  Review  │   Done   │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│ #125     │ #123     │ #PR-45   │ #PR-44   │ #122     │
│ Feature  │ Add      │ Portfolio│ Fix bug  │ Login    │
│ Request  │ Charts   │ API      │          │ Feature  │
│          │          │          │          │          │
│ #126     │ #124     │          │          │ #121     │
│ Improve  │ News     │          │          │ Setup    │
│ UI       │ Feed     │          │          │ Project  │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```

#### 자동화 설정

```
Settings → Workflows:

☑ Item added to project
  → Status: Backlog

☑ Item reopened
  → Status: To Do

☑ Pull request opened
  → Status: In Progress

☑ Pull request merged
  → Status: Done

☑ Issue closed
  → Status: Done
```

---

## 7. GitLab 시작하기

### 7.1 계정 생성

```
1. https://gitlab.com 접속
2. "Register" 버튼
3. 정보 입력:
   - Full name
   - Username
   - Email
   - Password (최소 8자)
4. 이메일 인증
```

### 7.2 프로젝트 생성

```
1. "New project" 버튼
2. "Create blank project" 선택
3. 정보 입력:
   - Project name: stock-prediction-system
   - Project slug: stock-prediction-system (URL)
   - Visibility Level:
     ● Private (비공개)
     ○ Internal (같은 GitLab 인스턴스)
     ○ Public (공개)
   - Initialize repository with a README
4. "Create project" 클릭
```

### 7.3 저장소 클론

```bash
# HTTPS
git clone https://gitlab.com/username/stock-prediction-system.git

# SSH (권장)
git clone git@gitlab.com:username/stock-prediction-system.git
```

### 7.4 GitLab 인터페이스

```
┌────────────────────────────────────────────────────────┐
│  username / stock-prediction-system                    │
├────────────────────────────────────────────────────────┤
│  [Project] [Repository] [Issues] [Merge requests]      │
│  [CI/CD] [Security] [Deployments] [Monitor]            │
├────────────────────────────────────────────────────────┤
│  main ▼  [Web IDE]  [+ ▼]  [Clone ▼]                  │
├────────────────────────────────────────────────────────┤
│  📂 backend/                                            │
│  📂 frontend/                                           │
│  📄 README.md                                           │
│  📄 .gitlab-ci.yml                                      │
└────────────────────────────────────────────────────────┘
```

---

## 8. GitLab에서 MR 만들기

### 8.1 MR vs PR

**용어만 다를 뿐 개념은 동일:**
- GitHub: Pull Request (PR)
- GitLab: Merge Request (MR)

### 8.2 MR 생성 과정

#### Step 1: 브랜치 생성 및 Push

```bash
git checkout -b feature/add-portfolio-api
# 코드 작성
git add .
git commit -m "feat: Add portfolio API"
git push origin feature/add-portfolio-api
```

#### Step 2: GitLab 웹에서 MR 생성

**방법 1: 푸시 후 링크**
```
출력:
remote: 
remote: To create a merge request for feature/add-portfolio-api, visit:
remote:   https://gitlab.com/username/stock-prediction-system/-/merge_requests/new?merge_request[source_branch]=feature/add-portfolio-api
```

**방법 2: 웹에서**
```
1. "Merge requests" 메뉴
2. "New merge request" 버튼
3. Source branch: feature/add-portfolio-api
4. Target branch: main
5. "Compare branches and continue"
```

### 8.3 MR 작성 화면

```
┌───────────────────────────────────────────────────────────┐
│  New merge request                                        │
├───────────────────────────────────────────────────────────┤
│  From feature/add-portfolio-api into main                 │
├───────────────────────────────────────────────────────────┤
│  Title                                                    │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ feat: Add portfolio creation API                    │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  Description                                              │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ ## What does this MR do?                            │ │
│  │ Implements portfolio creation feature               │ │
│  │                                                     │ │
│  │ ## Changes                                          │ │
│  │ - Add PortfolioController                           │ │
│  │ - Add PortfolioService                              │ │
│  │ - Add tests                                         │ │
│  │                                                     │ │
│  │ ## How to test                                      │ │
│  │ 1. Start the application                            │ │
│  │ 2. POST /api/portfolios                             │ │
│  │                                                     │ │
│  │ Closes #123                                         │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  Assignee: @myself                                        │
│  Reviewer: @john @sarah                                   │
│  Milestone: v1.2.0                                        │
│  Labels: enhancement, backend                             │
│                                                           │
│  ☑ Delete source branch when merge request is accepted   │
│  ☑ Squash commits when merge request is accepted         │
│  ☐ Mark as draft                                          │
│                                                           │
│  [Create merge request]                                   │
└───────────────────────────────────────────────────────────┘
```

### 8.4 GitLab MR 고급 기능

#### ① Approval Rules

**Settings → Merge requests → Merge request approvals:**

```
Approval rules:
┌─────────────────────────────────────────────────────┐
│ Rule name: Code review                              │
│ Approvals required: 2                               │
│ Eligible approvers:                                 │
│   @john-senior                                      │
│   @sarah-lead                                       │
│   @mike-backend                                     │
│                                                     │
│ ☑ Prevent approval by author                        │
│ ☑ Prevent approvals by users who add commits        │
└─────────────────────────────────────────────────────┘
```

#### ② Merge Checks

```
☑ Pipelines must succeed
☑ All threads must be resolved
☑ Status checks must succeed
☑ Merge requests must be approved by required reviewers
```

#### ③ Merge 전략 (GitLab)

```
● Merge commit (기본)
○ Merge commit with semi-linear history
○ Fast-forward merge
```

---

## 9. GitLab CI/CD

### 9.1 .gitlab-ci.yml

**GitLab의 CI/CD 설정 파일:**

```yaml
# .gitlab-ci.yml

stages:
  - build
  - test
  - quality
  - deploy

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"

cache:
  paths:
    - .m2/repository
    - backend/target

build:backend:
  stage: build
  image: maven:3.9-eclipse-temurin-17
  script:
    - cd backend
    - mvn clean compile
  artifacts:
    paths:
      - backend/target/
    expire_in: 1 hour

test:backend:
  stage: test
  image: maven:3.9-eclipse-temurin-17
  script:
    - cd backend
    - mvn test
  coverage: '/Total.*?([0-9]{1,3})%/'
  artifacts:
    when: always
    reports:
      junit:
        - backend/target/surefire-reports/TEST-*.xml
    paths:
      - backend/target/site/jacoco/

code_quality:
  stage: quality
  image: maven:3.9-eclipse-temurin-17
  script:
    - cd backend
    - mvn sonar:sonar
      -Dsonar.projectKey=stock-prediction-system
      -Dsonar.host.url=$SONAR_HOST_URL
      -Dsonar.login=$SONAR_TOKEN
  only:
    - merge_requests
    - main

deploy:staging:
  stage: deploy
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG .
    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG
  only:
    - merge_requests
  environment:
    name: staging
    url: https://staging.example.com
```

### 9.2 GitLab CI/CD 실행

**MR 생성 시 자동 실행:**

```
Pipeline #12345 for feature/add-portfolio-api

Stages:
🔵 build:backend        ✅ passed (1m 20s)
🔵 test:backend         ✅ passed (45s)
🔵 code_quality         ✅ passed (1m 10s)
🔵 deploy:staging       ⏳ running (30s)
```

---

## 10. GitHub vs GitLab 비교

### 10.1 기능 비교표

| 기능 | GitHub | GitLab |
|------|--------|--------|
| **Git 호스팅** | ✅ | ✅ |
| **PR/MR** | Pull Request | Merge Request |
| **CI/CD** | GitHub Actions | GitLab CI/CD (더 강력) |
| **Issue 관리** | GitHub Issues | GitLab Issues |
| **프로젝트 보드** | GitHub Projects | GitLab Boards |
| **Code Review** | ✅ 우수 | ✅ 우수 |
| **보안 스캔** | Dependabot, CodeQL | Security Dashboard |
| **Container Registry** | GitHub Packages | GitLab Container Registry |
| **Wiki** | ✅ | ✅ |
| **Self-hosted** | ❌ (Enterprise만) | ✅ (무료 포함) |
| **무료 Private 저장소** | 무제한 | 무제한 |
| **무료 CI/CD** | 2,000분/월 | 400분/월 |
| **커뮤니티** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **학습 곡선** | 쉬움 | 중간 |

### 10.2 언제 무엇을 선택?

#### GitHub 추천

```
✅ 오픈소스 프로젝트
✅ 빠른 시작 필요
✅ 커뮤니티 중요
✅ 간단한 CI/CD
✅ Microsoft 생태계
```

#### GitLab 추천

```
✅ 강력한 CI/CD 필요
✅ Self-hosting 필요
✅ 통합 DevOps 원함
✅ Kubernetes 중심
✅ 보안 중요 (규정 준수)
```

---

## 11. 협업 워크플로우

### 11.1 Git Flow

**브랜치 전략:**

```
main (프로덕션)
  ↓
develop (개발)
  ↓
feature/xxx (기능)
  ↓
release/x.x.x (릴리스)
  ↓
hotfix/xxx (긴급 수정)
```

**워크플로우:**

```
1. feature 브랜치에서 개발
   git checkout -b feature/add-portfolio develop

2. 개발 완료 후 PR/MR 생성
   base: develop ← compare: feature/add-portfolio

3. 리뷰 및 머지

4. 릴리스 준비
   git checkout -b release/1.2.0 develop

5. 릴리스 테스트 후 main과 develop에 머지

6. 긴급 버그 시
   git checkout -b hotfix/critical-bug main
   수정 후 main과 develop에 머지
```

### 11.2 팀 협업 규칙

#### Code Owners

**`.github/CODEOWNERS` 파일:**

```
# Backend
/backend/**           @john-backend @sarah-lead

# Frontend
/frontend/**          @mike-frontend @lisa-ui

# Database
/database/**          @john-backend

# CI/CD
/.github/workflows/** @sarah-lead
.gitlab-ci.yml        @sarah-lead

# Documentation
/docs/**              @tech-writer
*.md                  @tech-writer
```

**효과:**
- 파일 변경 시 자동으로 해당 소유자를 리뷰어로 추가
- 책임 명확화

#### PR/MR 사이즈 가이드

```
🟢 Small (< 300 lines)
   - 리뷰 시간: 15-30분
   - 권장 ✅

🟡 Medium (300-500 lines)
   - 리뷰 시간: 30-60분
   - 허용 ⚠️

🔴 Large (> 500 lines)
   - 리뷰 시간: 1-2시간
   - 분리 권장 ❌
```

---

## 12. 보안 및 권한 관리

### 12.1 GitHub 권한

**역할:**

| 역할 | 읽기 | 쓰기 | PR 생성 | PR 머지 | Settings |
|------|------|------|---------|---------|----------|
| **Read** | ✅ | ❌ | ❌ | ❌ | ❌ |
| **Triage** | ✅ | Issue/PR 관리 | ✅ | ❌ | ❌ |
| **Write** | ✅ | ✅ | ✅ | ✅ | ❌ |
| **Maintain** | ✅ | ✅ | ✅ | ✅ | 일부 |
| **Admin** | ✅ | ✅ | ✅ | ✅ | ✅ |

### 12.2 Branch Protection

**Settings → Branches → Add rule:**

```
Branch name pattern: main

Protect matching branches:
☑ Require a pull request before merging
  ☑ Require approvals (2)
  ☑ Dismiss stale pull request approvals when new commits are pushed
  ☑ Require review from Code Owners

☑ Require status checks to pass before merging
  ☑ Require branches to be up to date before merging
  Status checks:
    ☑ build-and-test
    ☑ code-quality

☑ Require conversation resolution before merging

☑ Require signed commits

☑ Require linear history

☑ Include administrators (관리자도 규칙 적용)
```

### 12.3 Secrets 관리

#### GitHub Secrets

```
Settings → Secrets and variables → Actions → New repository secret

Name: SONAR_TOKEN
Value: [토큰 값]
```

**워크플로우에서 사용:**

```yaml
- name: SonarCloud Scan
  env:
    SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
```

#### GitLab CI/CD Variables

```
Settings → CI/CD → Variables → Add variable

Key: SONAR_TOKEN
Value: [토큰 값]
☑ Protect variable (protected 브랜치에만)
☑ Mask variable (로그에 ***로 표시)
```

---

## 13. 바이브코딩과의 통합

### 13.1 AI 도구와 GitHub/GitLab

#### Claude Code

```
1. Claude에게 코드 생성 요청
2. 로컬에 저장
3. Git 커밋 & Push
4. GitHub/GitLab에서 PR/MR 생성
5. Claude에게 PR 설명 작성 요청
```

#### Cursor

```
1. Cursor에서 코드 작성
2. Cursor Source Control에서 커밋
3. Cursor에서 Push
4. GitHub/GitLab에서 PR/MR 생성
```

#### GitHub Copilot

```
1. VSCode에서 Copilot으로 코드 작성
2. VSCode Source Control에서 커밋
3. GitHub에 Push (통합 우수)
4. GitHub에서 PR 생성 (버튼 하나로 가능)
```

### 13.2 AI 생성 코드 PR 작성법

**PR 설명 템플릿:**

```markdown
## Description
[기능 설명]

## AI Tool Used
- Tool: Claude Code / Cursor / GitHub Copilot
- Model: Claude 3.5 Sonnet / GPT-4

## Initial Prompt
```
포트폴리오 생성 API를 만들어줘.
요구사항: ...
```

## Generated Files
- [ ] PortfolioController.java (AI 생성)
- [ ] PortfolioService.java (AI 생성)
- [ ] PortfolioMapper.java (AI 생성)
- [ ] PortfolioServiceTest.java (AI 생성)

## Manual Modifications
- [ ] Line 45: 중복 체크 로직 개선
- [ ] Line 89: 에러 메시지 한글화
- [ ] Test: Edge case 추가

## AI Code Review
AI가 생성한 코드를 다음 관점에서 검토했습니다:
- ✅ 비즈니스 로직 정확성
- ✅ 보안 취약점 확인
- ✅ 성능 이슈 확인
- ✅ 테스트 충분성

## Checklist
- [x] AI 코드 검증 완료
- [x] 테스트 추가/수정
- [x] 보안 검토 완료
```

---

## 14. FAQ

### Q1: GitHub과 GitLab 중 어느 것을 배워야 하나요?

**A:** 두 가지 모두 비슷하므로 하나만 배우면 됩니다.

```
추천 학습 순서:
1. GitHub 먼저 (더 쉽고, 커뮤니티 크고, 자료 많음)
2. 회사에서 GitLab 사용 시 추가 학습 (1-2주면 충분)

핵심 개념은 동일:
- Git 저장소
- 브랜치
- PR/MR
- CI/CD
- Issue 관리
```

### Q2: PR 리뷰를 얼마나 기다려야 하나요?

**A:** 팀 규칙에 따라 다르지만 일반적:

```
목표 응답 시간:
- Urgent (hotfix): 1-2시간
- Normal: 1-2일
- Low priority: 3-5일

리뷰어 부담:
- 하루에 2-3개 PR 리뷰 적정
- 각 PR당 30분-1시간

리뷰 독촉:
- 2일 지나면 슬랙/이메일로 리마인드
- 3일 지나면 다른 리뷰어 추가
```

### Q3: 여러 개의 커밋을 하나로 합치고 싶어요

**A:** Squash Merge 사용

```
방법 1: GitHub/GitLab에서
PR/MR 머지 시 "Squash and merge" 선택

방법 2: 로컬에서
git rebase -i HEAD~3  # 최근 3개 커밋
# pick → squash로 변경
git push -f origin feature-branch
```

### Q4: 실수로 main에 직접 push했어요

**A:** 되돌리기

```bash
# 1. 로컬에서 revert
git revert HEAD

# 2. 원격에 push
git push origin main

# 또는 강제로 되돌리기 (위험!)
git reset --hard HEAD~1
git push -f origin main

⚠️ 주의: 
- main은 보호되어야 함 (Branch Protection)
- 팀원과 협의 필요
```

### Q5: PR이 Conflict 났어요

**A:** 해결 방법

```bash
# Step 1: main 최신 코드 받기
git checkout main
git pull origin main

# Step 2: 내 브랜치로 돌아가기
git checkout feature/my-feature

# Step 3: main을 병합
git merge main

# Conflict 발생 시 파일 열어서 수동 해결:
<<<<<<< HEAD (내 코드)
내 변경사항
=======
main의 변경사항
>>>>>>> main

# Step 4: 해결 완료 표시
git add .
git commit -m "Merge main into feature/my-feature"

# Step 5: Push
git push origin feature/my-feature
```

### Q6: CI/CD가 너무 오래 걸려요

**A:** 최적화 방법

```yaml
# 캐시 사용
cache:
  paths:
    - node_modules/
    - .m2/repository

# 병렬 실행
jobs:
  test-backend:
    # ...
  test-frontend:
    # ...
# 동시 실행!

# 조건부 실행
on:
  pull_request:
    paths:
      - 'backend/**'  # backend 변경 시만 실행
```

### Q7: Private 저장소도 무료인가요?

**A:** 네, 둘 다 무료입니다.

```
GitHub:
- Private 저장소: 무제한 (무료)
- CI/CD: 2,000분/월 (무료)

GitLab:
- Private 저장소: 무제한 (무료)
- CI/CD: 400분/월 (무료)
- Self-hosting: 무료 (Community Edition)
```

### Q8: Self-hosting이 뭔가요?

**A:** 직접 서버에 설치

```
GitLab Community Edition:
- 자체 서버에 설치
- 완전한 제어권
- 데이터 소유권
- 보안/규정 준수
- 무료!

사용 사례:
- 금융 회사 (규정)
- 정부 기관 (보안)
- 대기업 (제어권)
```

### Q9: 오픈소스 프로젝트를 만들고 싶어요

**A:** GitHub 추천

```
1. Public 저장소 생성
2. LICENSE 파일 추가 (MIT, Apache 등)
3. CONTRIBUTING.md 작성
4. CODE_OF_CONDUCT.md 추가
5. README.md 작성
6. Issue 템플릿 설정
7. PR 템플릿 설정

GitHub의 장점:
- 더 큰 오픈소스 커뮤니티
- 발견되기 쉬움
- GitHub Sponsors (후원)
```

### Q10: 팀원을 초대하려면?

**A:** Collaborators 추가

**GitHub:**
```
Settings → Collaborators and teams
→ Add people
→ Username 입력
→ 역할 선택 (Read, Write, Admin)
→ Invite
```

**GitLab:**
```
Settings → Members
→ Invite members
→ Username 입력
→ 역할 선택 (Guest, Developer, Maintainer, Owner)
→ Invite
```

---

## 📝 요약

### GitHub & GitLab 핵심

**공통점:**
- Git 저장소 호스팅
- PR/MR (코드 리뷰)
- Issue 관리
- CI/CD
- 프로젝트 보드

**차이점:**
- GitHub: 커뮤니티, 오픈소스, 쉬움
- GitLab: DevOps, Self-hosting, 강력한 CI/CD

### PR/MR 만들기 핵심 단계

```
1. 브랜치 생성
2. 코드 작성
3. Commit & Push
4. 웹에서 PR/MR 생성
5. 제목, 설명 작성
6. Reviewers 지정
7. CI/CD 통과 대기
8. 리뷰 및 수정
9. Approve
10. Merge
```

### 바이브코딩 시대의 활용

- AI 도구로 코드 생성
- GitHub/GitLab에서 협업
- PR/MR로 품질 보장
- CI/CD로 자동화

### 핵심 메시지

> **GitHub/GitLab은 단순한 코드 저장소가 아닙니다.**
> **현대 소프트웨어 개발의 협업 플랫폼입니다.**

---

**작성자**: Claude  
**작성일**: 2025-12-07  
**문서 버전**: 1.0  
**대상**: 바이브코딩 개발자, 팀 협업 초보자

---

**관련 문서:**
- Pull Request 완전 가이드
- Git 기초 가이드
- CI/CD 자동화 가이드
- 팀 협업 워크플로우

---

**작성 일시**: 2025-12-07 02:15:28 (한국 시간 기준)
